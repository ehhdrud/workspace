import React from "react";
import ReactDOM from "react-dom";
import FollowButton from "./components/FollowButton";

const domContainer = document.querySelector("#root");
ReactDOM.createRoot(domContainer).render(<FollowButton />);
